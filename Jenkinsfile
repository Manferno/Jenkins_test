pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'}
                git branch: 'main', credentialsId: 'Llave_1', url: 'git@github.com:Manferno/Jenkins_test.git'
            }
        }
    }
}
